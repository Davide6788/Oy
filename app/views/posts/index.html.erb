<ul>
  <% @posts.each do |post| %>
    <li><%= post.content %></li>
  <% end %>
</ul>

<div class="container-form-new">
  <%= simple_form_for [ @business, @post] do |f| %>
    <div class="create-business-form">
      <%= f.input :content %>
      <%= f.submit "Post" %>
    </div>
  <% end %>
</div>
